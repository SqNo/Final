{% extends 'base.html.twig' %}
{% block body %}
<div class="container">
    <div class="row">
        <form action="{{ path('changePwd', {'token' :token}) }}" method="post">
            {{form_start(form)}}
            {# {{form_rest(form)}} #}
            <input type="submit" value="envoyer"></button>
            {{form_end(form)}}
            
        </form>
    </div>
    {# <a href="#">mot de passe oublié</a> #}
</div>
{% endblock %}